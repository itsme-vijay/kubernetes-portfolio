apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: correct-path-ingress
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: api-service
            port:
              number: 80
